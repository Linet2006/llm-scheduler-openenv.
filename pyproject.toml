[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "llm_scheduler"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "openenv-core",
]

[project.scripts]
server = "server.app:main"
